
"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // error because we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")
//documentation of alert=>tc39.es  ecmascript

let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => range is 2 to power 53
// bigint ->for large numbers
// string => ""
// boolean => true/false
// null => standalone value->representaiion for empty val(no val is recieved)
// undefined => variable is declared but no value is assigned now
// symbol => to find uniqueness


// object

console.log(typeof age); //number
console.log(typeof "nikhil"); //string
console.log(typeof undefined); // undefined
console.log(typeof null); // object
